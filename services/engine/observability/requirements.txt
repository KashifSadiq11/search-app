# Observability Module Dependencies
# These are optional - the module gracefully degrades without them

# OpenTelemetry (optional - for distributed tracing)
# opentelemetry-api>=1.23.0
# opentelemetry-sdk>=1.23.0
# opentelemetry-exporter-otlp>=1.23.0

# Note: Core dependencies (fastapi, starlette) are already in main requirements.txt
